# Generated test results

This directory is reserved for **local** outputs from tests or manual experiments (e.g. pytest tmp artifacts copied here for inspection).

- Default: **gitignored** (see `.gitignore` in this folder).
- Do not commit large CSV/JSON dumps from production runs.

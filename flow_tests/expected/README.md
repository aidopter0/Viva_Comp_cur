# Expected / golden references

Optional: store small **non-sensitive** checksums or JSON snippets for regression tests.

Production-scale golden files are intentionally not kept here to avoid repo bloat.

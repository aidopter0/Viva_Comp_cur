# Archived / reference only

- **`prepare_key_items_heuristic.py`** — legacy heuristic builder for `key_items_prepared.json`. The supported flow is `gemini_key_items_builder.py` → `config/key_items_prepared_gemini.json`.
- **`config/key_items_prepared.json`** — optional snapshot from the heuristic script (if generated).
- **`Key_SKUS_Competition_Pricing.xlsx`** — source workbook (not used by Python); kept here to declutter repo root.
- **`notes/`** — local capture files (e.g. network traces); review for secrets before sharing.
